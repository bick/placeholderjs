import MdxLayout from './mdx-layout'
import Head from 'next/head'

<Head>
    <title>My page title</title>
</Head>

# Getting Started
PlaceholderJS is a simple, lightweight JavaScript library that allows you to create placeholder images for your websites. It's easy to use and requires no dependencies.

<div className="flex border-t py-6 mt-6">Next article: <a href="/docs/installation" className="ml-2">Installation →</a></div>

export default function Docs({children}) {
    return <MdxLayout>{children}</MdxLayout>
}